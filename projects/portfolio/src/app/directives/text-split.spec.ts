import { TextSplit } from './text-split';

describe('TextSplit', () => {
  it('should create an instance', () => {
    const directive = new TextSplit();
    expect(directive).toBeTruthy();
  });
});
