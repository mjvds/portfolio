import { ProjectTemplate } from './project-template';

describe('ProjectTemplate', () => {
  it('should create an instance', () => {
    const directive = new ProjectTemplate();
    expect(directive).toBeTruthy();
  });
});
